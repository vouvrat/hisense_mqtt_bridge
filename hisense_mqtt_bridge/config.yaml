name: "Hisense TV MQTT Bridge"
version: "1.0.1"
slug: hisense_mqtt_bridge
description: "Bridge MQTT pour contrôler une TV Hisense VIDAA"
arch:
  - aarch64
  - amd64
  - armv7
init: false
startup: application
boot: auto
hassio_api: true
homeassistant_api: true

options:
  mqtt_broker: "192.168.1.7"
  mqtt_port: 1883
  mqtt_user: ""
  mqtt_password: ""
  mqtt_topic_prefix: "hisense_tv"
  tv_ip: ""
  tv_port: 36669
  tv_name: "salon"
  ssl_enabled: false
  auto_discovery: true
  scan_interval: 30
  log_level: "INFO"

schema:
  mqtt_broker: str
  mqtt_port: port
  mqtt_user: str?
  mqtt_password: password?
  mqtt_topic_prefix: str
  tv_ip: str
  tv_port: int(1,65535)
  tv_name: str
  ssl_enabled: bool
  auto_discovery: bool
  scan_interval: int(10,300)
  log_level: list(DEBUG|INFO|WARNING|ERROR)
